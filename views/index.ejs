<%- include('partials/header.ejs') %>
<body>
  <%- include('partials/navbar.ejs') %>
  
    <div class="container d-auto justify-content-center text-center">
      <h2 class="mt-3 text-center">Perguntas que você recebeu!</h2>
      <a href="/perguntar" class="btn btn-md">Perguntar</a>
      
      <% perguntas.forEach(pergunta => { %>
        <div class="card mt-3">
          <div class="card-body text-left">      
            <h3>
              <%= pergunta.titulo %>
            </h3>
          </div>
          <div class="card-footer text-left">
           <a href="/pergunta/<%= pergunta.id %>"><button class="btn btn-outline-dark btn-sm col-xs-2 mt-3">Responder</button></a>
          </div>
        </div>
      <% }) %>
    </div>  

</body>
  <%- include('partials/footer.ejs') %>
